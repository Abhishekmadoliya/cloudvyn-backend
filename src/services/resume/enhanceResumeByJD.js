import { generateWithOllama } from "../../utils/llm/ollama"

generateWithOllama
export const  enhanceResumeByJD = (resumeJson,JD)=>{
    try {
        ollamaRouter
    } catch (error) {
        
    }
}